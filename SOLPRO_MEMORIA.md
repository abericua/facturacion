# 🧠 MEMORIA DE TRABAJO SOLPRO - Actualizado 1 de Junio 2026

## 🎯 Estado del Proyecto (Sincronizado con SGSP Root)
El sistema ha evolucionado significativamente con un enfoque en **Seguridad (Protocolo 007)**, **Persistencia en la Nube (Railway)** y **Estabilidad Operativa**.

### ✅ Hitos Críticos de Mayo 2026
1.  **Seguridad & Autenticación (Protocolo 007):**
    *   Implementación de `SYSTEM_PEPPER` y SHA-256 para contraseñas.
    *   Doble factor de autenticación (2FA) obligatorio con reset de emergencia vía código maestro `007007`.
    *   Eliminación de login inseguro por parámetros de URL.
2.  **Infraestructura & Nube:**
    *   Configuración de **Railway Volume** (`/app/data`) para persistencia real de bases de datos y facturas emitidas.
    *   Lógica de "Bootstrap Inteligente" para evitar pérdida de datos al reiniciar el contenedor.
3.  **Lógica de Negocio & Stock:**
    *   **Validación de Stock en Tiempo Real:** Prevención de *Race Conditions* en ventas simultáneas.
    *   **Anulaciones Inteligentes:** Restauración automática de stock mediante `RAW_ITEMS` (JSON) en el Excel de ventas.
    *   **Regeneración de PDFs:** Capacidad de reconstruir facturas antiguas con coordenadas exactas (`x=550, y=490`).
4.  **UI/UX:**
    *   Diseño Responsivo (Mobile-Friendly) implementado en formularios y carritos.
    *   Integración nativa de la **Calculadora Solpro Elite v35.2** dentro del portal.

### 📋 Pendientes Críticos
- Monitorear la sincronización entre el volumen de Railway y el repositorio local.
- Validar la integridad del `SYSTEM_PEPPER` en nuevas migraciones de usuarios.

## 🤖 Notas entre Agentes (Handoff)
- **Gemini CLI:** He movido el protocolo a la raíz `SGSP/`. He escaneado las bitácoras y `main_portal.py` para sincronizarme con los cambios de seguridad (007) y persistencia (Railway).
- **Claude:** Fix aplicado en `app.py` línea 1094 — botón "Sincronizar Productos" fallaba con "Object of type DataFrame is not JSON serializable". Solución: `json.loads(df.to_json(orient='records'))` para convertir tipos numpy a Python nativos. Commit `4687ea4` pusheado por Gemini. `app_bridge.py` no existe en el proyecto — Gemini lo confirmó. El bridge real es `routes_bridge.py`.
- **Gemini CLI:** Se creó un servicio independiente en Railway (`bridge-api`) exclusivo para FastAPI usando `Dockerfile.api`. La URL base se actualizó a `https://facturacion-production-3916.up.railway.app` en `sync_service.py` y `mi-backoffice/.env`.
- **Claude (2026-06-03):** El ImportadorCompras / CargadorDocumentos / ConciliacionBancaria fallaban al "leer PDF". Causa real: NO era lectura de PDF (el texto se extrae en el navegador con pdf.js). El error `invalid x-api-key` venía de `api.anthropic.com` reenviado por el proxy `/api/bridge/anthropic/messages` → la `ANTHROPIC_API_KEY` en Railway estaba inválida (una key nueva tampoco funcionó, probable falta de billing/créditos en la cuenta Anthropic). **Decisión del usuario: migrar a Gemini.** Refactoricé `routes_bridge.py` → el endpoint `/anthropic/messages` ahora es un proxy LLM multi-proveedor (`proxy_llm`): si existe `GEMINI_API_KEY` (o `LLM_PROVIDER=gemini`) traduce el payload Anthropic→Gemini (`generateContent`) y devuelve la respuesta en shape Anthropic (`content:[{text}]`), así el frontend YA compilado no necesita cambios ni rebuild. Anthropic queda como fallback. **PENDIENTE en Railway:** setear `GEMINI_API_KEY` (y opcional `GEMINI_MODEL`, default `gemini-2.0-flash`) en el servicio `bridge-api` y redeploy.
- **Antigravity (2026-06-03):** Cambios de Claude en `routes_bridge.py` verificados, commiteados y pusheados (commit `41278421`). El `.git/index.lock` trabado fue eliminado. Railway detectó el cambio y está corriendo un deploy nuevo en el servicio `bridge-api`. El proxy LLM con soporte nativo para Gemini ya está en producción.
- **Claude (2026-06-09):** Fix en `ImportadorCompras.jsx` línea ~253 — error `Expected ',' or '}' after property value in JSON at position 290` al procesar PDFs kuDE (ej: `CC14019-00100100049295-50.pdf`). Causa: Gemini vía proxy devuelve JSON con sintaxis inválida (trailing commas, texto extra, números con separador paraguayo `7.650.000`). Solución: parsing en 3 intentos — (1) `JSON.parse` directo, (2) extracción del bloque `{...}` más externo con regex, (3) sanitización de problemas comunes del LLM (trailing commas, `True/False/None`, números con puntos como miles). **Requiere rebuild del frontend** (`npm run build` en `mi-backoffice/`).
- **Claude (2026-06-09 — continuación):** Mejora mayor en `ImportadorCompras.jsx` — se reemplazó el `SYSTEM_PROMPT` genérico por `getSystemPrompt(proveedorId)` que inyecta parámetros financieros por proveedor extraídos del catálogo `productos_maestros.csv`. Sol Control → `moneda=USD, prefijo SC-`, Todo Costura → `moneda=PYG, prefijo TC-`. También: (1) `processOne` ahora recibe `proveedorHint` (el filtro activo del usuario), (2) validación post-extracción corrige moneda si el LLM se equivoca, (3) para facturas PYG los campos `precio_unit_usd/subtotal_usd` se fuerzan a `null`. **Requiere rebuild del frontend.**
- **Claude (2026-06-09 — fix parseo v3):** Bug crítico: regex greedy `\{[\s\S]*\}` capturaba texto posterior al JSON si ese texto contenía `{}` (ej: RUC `{80054996-1}` en una nota). Causa del error `Unexpected non-whitespace character after JSON at position 826`. Fix: reemplazado por `extractFirstJSON()` que recorre char a char y devuelve el primer bloque `{}` perfectamente balanceado, ignorando todo lo que venga después. **Requiere rebuild.**
- **Claude (2026-06-09 — fix sync_stock):** `routes_bridge.py` → `sync_stock` reforzado para CSV corrupto o columnas faltantes. Antes: si el CSV existía pero le faltaba la columna "Nombre" (u otra), crasheaba con `KeyError`. Ahora: se itera sobre `BASE_COLS` y se agrega cualquier columna faltante antes de procesar. También el `pd.read_csv` está envuelto en try/except para manejar CSV malformado. **Commit `ae1f6437` listo — PENDIENTE PUSH por Antigravity** (`git push origin main`).
- **Claude (2026-06-09 — fix sync cross-device):** Problema: datos cargados en notebook A no aparecían en notebook B porque todo vivía solo en IndexedDB local. Fix en dos archivos: (1) `ImportadorCompras.jsx` línea ~504 — después de `DB.guardarCompras(allRecords)` se agrega `await SyncBridge.pushCompras()` para que las compras queden en Railway. (2) `backoffice.jsx` línea ~938 — nuevo `useEffect` en el componente `BackOffice` que llama `SyncBridge.pullAll()` al montar, hydratando IndexedDB desde Railway en cualquier dispositivo que abra el dashboard. **Requiere rebuild del frontend.**
- **Claude (2026-06-09 — fix deploy dist):** El build con sync cross-device existía localmente pero el `dist/` no estaba commiteado — Railway servía el JS viejo sin `pullAll()`. Fix: Antigravity commiteó (`d1060f70`) el nuevo `index-CbRNR8rP.js` y eliminó el viejo `index-wtSuov70.js`. Después del redeploy, `dashboard.solpropy.com` hydrata IndexedDB desde Railway al iniciar automáticamente.
- **Claude (2026-06-15 — Migración a PostgreSQL completa):** El usuario eligió "la 2" (migrar todo a Postgres como fuente única de verdad para resolver el problema de datos que no persisten entre PCs). Se reescribieron 3 archivos:
  1. **`db_sgsp.py`** — Reescrito completamente. Ahora crea y maneja TODAS las tablas: `sgsp_pedidos`, `sgsp_pagos`, `sgsp_pedido_items`, `sgsp_clientes`, `sgsp_productos`, `sgsp_tipo_cambio`, `sgsp_tipo_cambio_historico`, `sgsp_compras`, `sgsp_iva_mensual`, `sgsp_ventas`. `init_db()` es idempotente (`CREATE TABLE IF NOT EXISTS`).
  2. **`routes_bridge.py`** — Reescrito para usar `db_sgsp` en lugar de archivos. Mismo contrato de API exacto (mismas rutas, mismos response shapes). El frontend compilado NO requiere ningún cambio. El proxy LLM (Gemini/Anthropic) no cambió.
  3. **`migrate_all.py`** — Script de migración única creado. Lee todos los JSON/CSV/Excel existentes y los inserta en Postgres. Soporta `--dry-run` y `--solo entidad`. **Debe ejecutarse UNA VEZ en Railway Console**: `python migrate_all.py`.
  4. **`api.py`** — Modificado startup: ahora llama `db_sgsp.init_db()` al arrancar, además de `inicializar_base_datos()` (SQLite legacy).
  **RESUELTO (Claude + Antigravity - 2026-06-15):** Commits `6bd66648` (migración PG) + `fdaadde0` (fix NaN). Endpoint `/api/bridge/ventas` retorna 253 registros correctamente. Causa raíz: celdas vacías en `PRECIO USD` del Excel → pandas las convierte a `float('nan')` → psycopg2 las inserta como NaN en Postgres → `json.dumps` no puede serializar NaN → 500 crudo (sin `{"detail":...}`). Fix en `db_sgsp.py`: (1) `get_ventas()` sanitiza NaN/Inf → `None` al leer (cubre datos ya en BD), (2) helper `_safe_float()` en `sync_ventas_bulk()` previene insertar NaN en el futuro. No fue necesario re-migrar.
- **Claude (2026-06-15 — fix Dashboard race condition + NaN compras):** Commit `1cfb5adf`. (1) `db_sgsp.get_compras()` ahora sanitiza NaN/Inf → `None` igual que `get_ventas()`. (2) Race condition en `DashboardReal2026.jsx`: el Dashboard leía `DB.obtenerTodasCompras()` antes de que `SyncBridge.pullAll()` (iniciado en BackOffice mount) terminara → veía vacío y nunca recargaba. Fix: useEffect fusionado suscribe a `SyncBridge.subscribe()` y recarga compras+IVA cuando `status === 'ok'`. Se importó SyncBridge. **PENDIENTE del usuario:** `sgsp_compras` tiene 0 filas en Railway. Ir a ImportadorCompras en el dispositivo que tiene los datos → click Guardar/Confirmar → `SyncBridge.pushCompras()` sube todo a Railway → desde cualquier dispositivo el pullAll al abrir trae las compras.
- **Claude (2026-06-18 — fix extracción PDF con layout espacial):** Bug root-cause confirmado: `content.items.map(item => item.str).join(' ')` en pdfjs ignora coordenadas reales de los items → columnas de tablas kuDE/SIFEN se mezclan → Gemini lee mal los valores. Fix aplicado en **3 archivos**: `ImportadorCompras.jsx` (`extractPdfText`), `CargadorDocumentos.jsx` (`extraerTextoPDF`), `ConciliacionBancaria.jsx` (`extractPdfText`). Nueva lógica: (1) filtra items vacíos, (2) mapea `transform[4]=X` y `transform[5]=Y`, (3) ordena Y desc (arriba primero en coords PDF) + X asc dentro de misma fila, (4) agrupa en filas con tolerancia 3pt en Y, (5) une cada fila con doble espacio y filas con `\n`. `getTextContent({ normalizeWhitespace: false })` para preservar espaciado original. Auditoría Dashboard: ✅ endpoints `/ventas/csv` y `/productos/csv` existen, ✅ DB.obtenerConfig/guardarConfig/obtenerCatalogo/guardarCatalogo/obtenerTodasCompras/obtenerIVAPorAnio todos presentes, ✅ SyncBridge.onStatusChange correcto con unsub, ✅ race condition ya corregida. Build `npx vite build --emptyOutDir false` → ✓ 2346 módulos, sin errores. **Requiere redeploy en Railway (`solpro-dashboard`).**
- **Claude (2026-06-09 — fix parseo avanzado):** Nuevos tipos de error JSON cubiertos en `ImportadorCompras.jsx`: (1) claves sin comillas `{key: val}` → se quotean, (2) ellipsis `...` fuera de strings (LLM abrevia) → se eliminan/nullifican, (3) JSON truncado por límite de tokens → `closeJSON()` cierra llaves/corchetes abiertos contando el stack, (4) string sin cerrar al final → agrega comilla faltante, (5) newlines dentro de strings → se reemplazan por espacios. Lógica reorganizada en `sanitizeLLMJson()` (intento 3) + `sanitizeAgressive()` + `closeJSON()` (intento 4). También `max_tokens` subido de 4000 → 8000 para facturas con muchos ítems. **Requiere rebuild.**
- **Antigravity + Claude (2026-06-17 — Deploy frontend React):** Se descubrió que `mi-backoffice` nunca tuvo servicio Railway propio. `dashboard.solpropy.com` apuntaba (por error) al backend FastAPI (`facturacion-production-64cb`). Se creó nuevo servicio `solpro-dashboard` en Railway con Root Directory `mi-backoffice` y Dockerfile nginx. URL: `solpro-dashboard-production.up.railway.app`. CNAME `dashboard` en cPanel debe actualizarse a esta URL. Custom Domain `dashboard.solpropy.com` pendiente de agregar en Railway → Settings → Networking.
- **Antigravity (2026-06-17 — Deploy commit `738442e5`):** Commiteados y pusheados a `main` los cambios de Claude (Tasks #10, #11, #12). Nota: `app_bridge.py` no existe — el bridge real es `routes_bridge.py`. `GEMINI.md` corregido por Claude.
- **Claude (2026-06-21 — Auditoría + Fix completo sistema de facturación):** Sesión de emergencia. Auditoría de todos los archivos del sistema. 5 bugs críticos identificados y corregidos. Todos los archivos compilados con `py_compile` sin errores.
  - **[CRÍTICO - CORREGIDO] `app.py` línea 306 — `LOGO_FILE` apuntaba a directorio padre del proyecto.** Fix: `os.path.join(BASE_DIR, ...)` en lugar de `os.path.join(os.path.dirname(BASE_DIR), ...)`.
  - **[CRÍTICO - CORREGIDO] `app.py` líneas 560-594 — `validate_stock()` siempre retornaba `True` (código muerto después del return).** Reescrita completamente: ahora consulta `db_sgsp.get_productos(solo_activos=False)`, construye mapa de stock con `id_solpro` y `codigo_proveedor`, y bloquea ventas cuando `cantidad_solicitada > stock_disponible`. Cualquier error de DB resulta en permiso (no bloquea ventas).
  - **[CRÍTICO - CORREGIDO] `generador_facturas.py` línea 115 — `base_path` hardcodeado a `C:\Users\solpr\Desktop\Creador de Facturas`.** Fix: `os.path.dirname(os.path.abspath(__file__))`. Además se agregó verificación `os.path.exists(plantilla)` y `os.startfile` reemplazado por lógica multiplataforma (`subprocess + platform`).
  - **[ADVERTENCIA - CORREGIDA] `pdf_generator.py` — `import sqlite3`, `DATA_DIR`, `DB_PATH` y funciones `generate_invoice_pdf_from_db()` / `_obtener_datos_factura_db()` eran dead code apuntando a `sgsp_master.db` (no existe, sistema migró a PostgreSQL).** Todo eliminado. Solo queda `generate_invoice_pdf(data, output_path)` que es la función activa.
  - **[ADVERTENCIA - CORREGIDA] `routes_bridge.py` líneas 356-359 — Ruta duplicada `@router.get("/ventas/csv")` con cuerpo `pass`.** Eliminada. Solo queda la implementación real en línea 173.
  - **EXTRA:** Null bytes al final de `app.py`, `pdf_generator.py` y `routes_bridge.py` eliminados (eran pre-existentes, impedían `py_compile` en Linux/Railway).
- **Claude (2026-06-17 — Tareas #10, #11, #12: Refactor técnico completo del backoffice):**
  - **#10 — `src/theme.js`:** Extraído `const T` duplicado de 8 archivos (`backoffice.jsx`, `CalculadoraPrecios.jsx`, `CargadorDocumentos.jsx`, `ConciliacionBancaria.jsx`, `DashboardReal2026.jsx`, `FinanzasPro.jsx`, `ImportadorCompras.jsx`, `VentasAnalytics.jsx`). Cada uno ahora hace `import T from './theme.js'`. Fuente única de verdad.
  - **#11 — `src/constants.js`:** Creado con `CFG_DEFAULT` (márgenes %, buffers, comisión QR, redondeo), `MARGENES_MULT` (multiplicadores ×1.30/1.24/1.15), `MARGEN_DEFAULT`, `BANDA_PISO_PTS=150`, `BANDA_TECHO_PTS=350`, `RECARGO_DIGITAL=0.04`. `CalculadoraPrecios.jsx` y `DashboardReal2026.jsx` importan desde ahí — eliminadas todas las constantes locales duplicadas.
  - **#12 — Fixes `backoffice.jsx`:** (1) "P&L 2024" → "P&L 2025". (2) Migración DB ya estaba en `useEffect` (ADVERTENCIA-4 ya resuelta). (3) Aviso visible cuando `filtered.length > 200` en tabla Pedidos — fila ámbar con count exacto y sugerencia de búsqueda. Se eliminó aviso duplicado que existía de sesión anterior.
- **Claude (2026-06-17 — Tarea #9: Dashboard real desde IndexedDB):** Campaña de 9 bugs/mejoras en `mi-backoffice/src/backoffice.jsx` completada. En esta sesión: (1) Eliminados 3 arrays de demo del nivel de módulo: `inventory` (dead code — `Inventario` ya tenía su propio estado local), `suppliers` (idem), `invoices` (reemplazado por estado real). (2) Agregados helpers de categorización `CAT_COLORS_DASH`, `categorizarVenta()`, `derivarCategorias()` que parsean el CSV de ventas desde `_cache_ventas_csv` en IndexedDB para derivar el mix de categorías por unidades vendidas. (3) `Dashboard` convertido a componente stateful: `[catData, setCatData]` inicializa con `categoryData` estático (fallback 2025) y se reemplaza con datos reales si el CSV existe en IndexedDB. (4) `Finanzas` convertido a stateful: `[invoices, setInvoices]` carga las últimas 30 compras tipo FAC desde `compras_proveedores` store, convirtiendo `DD/MM/YY` a fecha legible y USD+PYG a monto en guaraníes. Las 3 tarjetas de resumen (CxC, CxP, Cartera Vencida) ahora tienen conteos dinámicos. `revenueData` y `orders` se mantienen estáticos (son datos reales 2025 y clientes reales con ciudades paraguayas). **NO requiere rebuild manual — el vite dev server recarga automáticamente.**

- **Claude / Antigravity (2026-06-23 — Sincronización: 3 bugs corregidos y pusheados a Railway):** Commit `6bf7a6f8`. Se identificaron y arreglaron 3 bugs críticos en el sistema de sincronización Cloud.
  - **FIX 1 (`sync_service.py`):** "Object of type datetime is not JSON serializable". La función `clean_keys()` no manejaba objetos datetime/date. Se agregó `elif hasattr(data, 'isoformat')`. Arregló crasheo en "Sincronizar Clientes". Se completó función truncada `sync_productos_bulk`.
  - **FIX 2 (`app.py`):** Productos nuevos del CSV no aparecían en facturación porque "Sincronizar Productos" no limpiaba el cache de `@st.cache_data`. Se agregó `st.cache_data.clear()` forzando re-lectura.
  - **FIX 3 (`db_sgsp.py`):** Stock se borraba al hacer sync de catálogo. `bulk_upsert_productos` tenía ON CONFLICT DO UPDATE SET stock_actual = EXCLUDED.stock_actual pero recibía la columna STOCK en mayúsculas. Se eliminaron stock_actual/reservado/disponible del ON CONFLICT. El stock SOLO se modifica via Tab STOCK, /stock/sync y venta/anulación. Se limpiaron 21 bytes nulos al final de db_sgsp.py.
  - **BONUS (`routes_bridge.py`):** Archivo truncado al final en `proxy_llm`. Se completó el bloque con fallback de texto. Todo en rama main con autodeploy.

- **Claude (2026-06-23 — Nuevo flujo: Calculadora → Facturación):** Commit `a130cfba`. Los productos nuevos agregados en la Calculadora ahora llegan a Facturación. Se agregó botón "Guardar en Servidor" en `calculadora_precios.html` que POSTea todos los productos a `/api/bridge/productos/sync`. En `app.py`, la función `load_products()` ahora consulta PostgreSQL y agrega productos cuyo `id_solpro` NO está en el CSV, calculando precios igual que los del CSV, apareciendo en el dropdown de Facturación. Es estrictamente aditivo: no toca productos del CSV. Además, se restauraron 38 líneas truncadas del final de `app.py`. Estado general del proyecto: ESTABLE.

## 🧠 Equipo de Trabajo (Multi-Agente)
- **Gemini CLI (Antigravity):** Arquitecto y ejecutor principal.
- **Claude Code:** Especialista en refactorización y UI avanzada.
- **Gemma 4 (Local):** Auditor de seguridad vía Bridge (:5005).
- **Usuario:** Dirección estratégica.

## 🗺️ Mapa de URLs y Rutas (FUENTE DE VERDAD)

> ⚠️ Antes de tocar cualquier endpoint, consultá esta sección.

### Servidor Railway (Backend)
- **Host Original:** `https://solpro-master-tec-production.up.railway.app`
- **Host Nuevo (bridge-api):** `https://facturacion-production-3916.up.railway.app`
- **Prefijo de todas las rutas:** `/api/bridge/` ← SIEMPRE bridge, NUNCA sgsp
- **Definido en:** `routes_bridge.py` línea 36

### Endpoints disponibles
| Método | Ruta | Desde | Descripción |
|--------|------|-------|-------------|
| GET | `/api/bridge/status` | JS + PY | Health check |
| GET | `/api/bridge/ventas` | JS | Ventas |
| GET | `/api/bridge/compras` | JS | Compras |
| GET | `/api/bridge/productos` | JS + PY | Leer productos CSV |
| GET | `/api/bridge/iva/{anio}` | JS | IVA por año |
| GET | `/api/bridge/dashboard/resumen` | JS | Resumen financiero |
| POST | `/api/bridge/compras/sync` | JS | Push compras |
| POST | `/api/bridge/iva/sync` | JS | Push IVA |
| POST | `/api/bridge/productos/sync` | JS + PY | Push productos CSV |
| POST | `/api/bridge/ventas/upload` | JS | Upload ventas |
| POST | `/api/bridge/anthropic/messages` | JS | Proxy seguro a Anthropic (CORS Fix) |
### Constantes de URL por archivo
| Archivo | Variable | Valor correcto |
|---------|----------|----------------|
| `sync_service.py` | `API_BASE_URL` | `...railway.app/api/bridge` |
| `mi-backoffice/src/SyncBridge.js` | `BRIDGE_URL` | `...railway.app` (sin prefijo, se agrega por ruta) |

### Frontend
- **Producción:** `https://facturacion.solpropy.com` (UI, no API)
- **Local React:** `http://localhost:5173` o `5174`
- **Local Streamlit:** `http://localhost:8501`

- **Claude (2026-06-24 — Fix regresión Antigravity en facturacion.solpropy.com):** Commit `3ea99583`. Antigravity había cambiado el `Dockerfile` para correr `main_portal.py` en vez de `app.py` — eso causó dos síntomas: (1) estética diferente (son dos apps distintas), (2) login roto (`main_portal.py` no tiene el override de emergencia de `app.py` y el hash del admin no correspondía a ninguna contraseña conocida). Fix: (1) `Dockerfile` revertido → `streamlit run app.py`. (2) `database/usuarios.json` → hash del admin actualizado a `SHA256("admin123"+PEPPER)` = `d5d14ea8...`. vendedor1 sigue con password `"1234"` (hash `7fcd87ad...`). totp_secrets vacíos en todos → usuarios deben escanear QR nuevo al primer login. **⚠️ Para Antigravity:** NO cambiar el `Dockerfile` para usar `main_portal.py`. El entry point de Railway es siempre `app.py`. `main_portal.py` existe pero NO es el entry point del servicio `solpro-facturacion`.
- **Antigravity (2026-06-25 — Fix Crítico localStorage en Calculadora y Setup Railway):** Se solucionó el problema de pantalla en blanco en la página de facturación/calculadora. Causa: Memoria del navegador (`localStorage`) corrupta causaba un crash al inicializar la variable `savedDb`. Fix: Se aplicó un parche en `static/calculadora_precios.html` y `Calculadora de precios solpro/calculadora_precios.html` agregando la validación `if (!Array.isArray(savedDb)) savedDb = [];` en el bloque `try-catch` inicial. Estos cambios fueron directamente desplegados a producción en Railway mediante `railway up -d`. Adicionalmente, se ejecutó la configuración de herramientas de Railway (`railway setup agent -y`) para facilitar la integración MCP y skills de Railway a los agentes (Claude Code, etc).
- **Claude (2026-06-25 — Fix CRÍTICO: tabla de productos vacía en Calculadora):** Síntoma reportado por el usuario: en la Calculadora desaparecieron TODOS los productos (tabla vacía, ni los 218 del `MASTER_DATA` interno). **Causa raíz (NO era localStorage):** el commit `e4d204f1` de Antigravity dejó el `<script>` con un **error de sintaxis JS** → `node --check` arrojaba `SyntaxError: Unexpected end of input`. Al fallar el parseo, el navegador NO ejecuta NADA del script → tabla vacía y botones muertos. El defecto exacto: quedó un fragmento muerto y sin cerrar de la vieja función `loadMasterData` (líneas 1026-1030) pegado antes del nuevo `window.onload` — un `function loadMasterData(savedMargenes){ ... if(savedMargenes){` que nunca se cierra (faltaban 2 `}`), y `loadMasterData` ya no se llama desde ningún lado (la lógica de carga vive dentro del propio `window.onload`). **Fix:** se borraron esas 5 líneas muertas en AMBOS archivos (`static/calculadora_precios.html` y `Calculadora de precios solpro/calculadora_precios.html`). Verificado con `node --check` → ✅ sin errores en los dos. **Se preservó intacta** la línea `if (!Array.isArray(savedDb)) savedDb = []` que arregló la pantalla en blanco (1 ocurrencia confirmada por archivo). Es el único cambio; no se tocó nada más. **PENDIENTE:** (1) deploy a producción (`railway up -d`). (2) El mojibake cosmético del mismo commit `e4d204f1` ("PARÁ¡METROS", "ðŸŸ¢ Banda Piso", BOM + `ó`→`Ã³`) sigue presente; el archivo local está limpio en UTF-8 pero la versión desplegada no — pendiente reguardar como UTF-8 limpio si se quiere corregir la estética.
- **Claude (2026-06-25 — Fix mojibake UTF-8 en Calculadora):** Continuación del fix anterior. Antigravity desplegó el fix de sintaxis vía `railway up -d` pero (1) el `git commit` NO se hizo — quedó trabado por un `.git/index.lock` (NO era "git no disponible"), y (2) solo quitó el BOM, dejando intacto el mojibake interno (10 ocurrencias de `Ã³`/`PARÃ` por archivo). Claude corrigió el mojibake de verdad con `ftfy.fix_text()` (maneja mojibake mixto sin tocar texto ya limpio) en ambos archivos. Resultado verificado: acentos correctos ("Parámetros", "Dólar Mercado", "Línea", "Producción"), emojis restaurados (🟢🔵📦), 0 mojibake restante, sin BOM, `node --check` ✅ OK, `Array.isArray` preservado (1/archivo). **PENDIENTE Antigravity:** liberar lock + commit + push + deploy: `del .git\index.lock` → `git add` los 2 archivos → `git commit` → `git push origin main` → `railway up -d`. **⚠️ El fix sigue SIN estar en el repo (último commit del repo = `e4d204f1`, el roto); si Railway autodespliega desde git, vuelve el bug.** NO tocar `window.onload` ni reintroducir `loadMasterData`.
- **Claude (2026-06-25 — Fix PDF facturas: ítems fuera de línea / colapsados):** Síntoma: al crear/regenerar facturas el PDF metía todos los productos en una sola fila (Cant "1", descripciones concatenadas "2 CYAN, 2 YELLOW, 2 HDK", precio unitario = total). **Causa:** el camino de REGENERAR PDF ANTIGUO (`app.py` ~1740) pasaba la `DESCRIPCION` histórica (que se guarda junta con `", ".join(f"{cant} {desc}")`, línea 1555) como UN solo ítem `{c:1, d:desc, p:total, t:total}`. El historial NO guarda precio por línea (`RAW_ITEMS` solo tiene código + cantidad). El camino de creación nueva (`app.py` ~1545) ya armaba un ítem por producto correctamente. **Fix (2 archivos):** (1) `pdf_generator.py` — `_dibujar_canvas` ahora (a) solo dibuja precio/total de línea si `>0` (celdas en blanco cuando no hay precio por línea), y (b) acepta `data['total_override']` para forzar el total real. Backward-compatible: facturas nuevas (con precio>0) no cambian. (2) `app.py` regen (~1740) — separa la `DESCRIPCION` por `", "`, parsea la cantidad inicial con regex `^(\d+)\s+(.*)$`, arma un renglón por producto con `p:0,t:0` y pasa `total_override=total`. **Verificado visualmente** renderizando ambos escenarios (nueva 3 ítems con precio, y regen sin precio por línea) → cada producto en su renglón, totales correctos (5.292.000). **PENDIENTE Antigravity:** `py_compile app.py pdf_generator.py` antes de deploy → commit → push → `railway up -d` en el servicio de facturación (entry point `app.py`). Borrar PNGs de prueba `nueva-1.png` / `regen-1.png` de la raíz si quedaron.

---

## 📈 Constantes Financieras SOLPRO (2026)
- **CMV:** 81.9%
- **Gastos Operativos:** 8.1%
- **Utilidad Neta:** 10.0%

